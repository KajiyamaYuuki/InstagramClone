<h1>以下の内容で送信する</h1>
<%= form_with(model: @picture, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p><%= @picture.content %></p>
  <%= form.submit "送信する" %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>
